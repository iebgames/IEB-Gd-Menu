#include "custom.hpp"

namespace eclipse::gui {
    // CustomComponent is mostly implemented in the header as it's a simple wrapper for a lambda.
    // This file is here for consistency and to avoid potential linker issues with virtual tables.
}
